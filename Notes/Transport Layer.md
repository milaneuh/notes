The transport layer is where the computer transform the packet data received from other hosts into protocols readable for it's applications. 

The two main protocols are the [[TCP|Transmission Control Protocol]] and the [[UDP|User Diagram Protocol]].