The Socket Statistic (ss) command, it is an useful command to display informations about network sockets. The command provides details about TCP, UDP and other socket types, including their states and associated processes.

Example: List All listening UDP and TCP sockets

Command :
```
ss -ltu
```
