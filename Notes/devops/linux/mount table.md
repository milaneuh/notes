The mount table is the kernel's list of which filesystem is attached  at which directory or [[mount point]]  



## Cards
Q: what is the mount table?
A: the kernel's list of which filesystem is attached at which directory (mount point).
