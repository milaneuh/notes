[[mount point|mounting]] over a directory hides its content rather than deleting them, they come back when unmounted. 

## Cards
Q: what happens to a directory's content when you mount over it?
A: it is hidden, not deleted. The content comes back when you unmount.
