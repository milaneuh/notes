To know if a directory is a [[mount point]], you can run `mountpoint <path>` and it will tell you if the directory is a mount point or not. 
## Cards
Q: how do you know if a directory is a mount point?
A: `mountpoint <path>`
